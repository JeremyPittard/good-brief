import { useState } from "react";

const Subscribe = () => {
  return (
    <iframe
      src="https://goodbrief.substack.com/embed"
      scrolling="no"
      loading="lazy"
      className="mx-auto"
    ></iframe>
  );
};

export default Subscribe;
